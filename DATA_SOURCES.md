# 股票筛选器 - 数据源与抓取方式

## 数据架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    数据获取层                             │
│  mootdx(通达信) │ 新浪API │ 腾讯API │ 种子缓存 │ 同花顺  │
└─────────────────┬───────┬────────┬──────────┬─────────┘
                  │       │        │          │
┌─────────────────▼───────▼────────▼──────────▼─────────┐
│                    数据缓存层                           │
│  ebit_cache.json │ chip_cache.json │ industry_stocks   │
└────────────────────────┬──────────────────────────────┘
                         │
┌────────────────────────▼──────────────────────────────┐
│                   筛选引擎                              │
│  5660只A股 → 实时行情 → 多维度筛选 → 排序分页           │
└────────────────────────────────────────────────────────┘
```

---

## 1. 全A股列表

| 项目 | 内容 |
|------|------|
| **数据源** | **通达信行情服务器 (TCP 7709)** |
| **依赖库** | `mootdx` (Python) |
| **获取方式** | `mootdx.quotes.Quotes.factory(market='std').stock_all()` |
| **原始数据** | DataFrame 含字段: code, volunit, decimal_point, name, pre_close |
| **过滤规则** | 排除B股(200/900开头)、指数(395/399/880-889)、基金/ETF/债券/REIT |
| **有效结果** | ~5,660 只（沪主板2,312 + 深主板1,008 + 创业板1,421 + 中小板919） |
| **缓存策略** | 服务启动时加载，1小时内复用 (`ALL_A_STOCKS_CACHE`) |
| **更新频率** | 每次服务重启自动刷新；或每隔1小时自动重载 |
| **代码位置** | `backend/server.py` → `get_all_a_stocks()` (行115-145) |

**备选方案**（当mootdx不可用时）：
- 种子缓存合并 (`_fallback_all_stocks()` → `data/industry_stocks.json`)

---

## 2. 实时行情（股价/PE/PB/市值等）

| 项目 | 内容 |
|------|------|
| **数据源** | **腾讯财经 API** |
| **API端点** | `https://qt.gtimg.cn/q=sh600519` (单只) / `q=sh600519,sz000001,...` (批量) |
| **编码** | GBK |
| **返回格式** | `v_sh600519="1~贵州茅台~600519~...~"` (88个~分隔字段) |
| **关键字段索引** | 3=最新价, 4=昨收, 32=涨跌幅%, 38=换手率%, 39=PE(TTM), 44=总市值(亿), 46=PB, 48=量比 |
| **批量限制** | 每批最多200只，通过URL逗号拼接 |
| **更新频率** | **实时**（每次用户点击筛选时拉取） |
| **代码位置** | `backend/server.py` → `tencent_batch_quote()` (行205-225) |

---

## 3. EBIT数据（利润表）

| 项目 | 内容 |
|------|------|
| **数据源** | **新浪财经财报API** |
| **API端点** | `https://quotes.sina.cn/cn/api/openapi.php/CompanyFinanceService.getFinanceReport2022?paperCode=sh600519&source=lrb&type=0&page=1&num=6` |
| **返回格式** | JSON，含 `result.data.report_list` 按报告期分组 |
| **关键字段** | `营业总收入`, `营业利润`, `净利润`, `财务费用`, `所得税费用` |
| **EBIT计算** | EBIT ≈ **营业利润**（中国会计准则最佳近似） |
| **EBIT利润率** | EBIT / 营业总收入 × 100% |
| **报告期优先级** | 最新年报 > 最新中报 > 最新季报 |
| **缓存文件** | `data/ebit_cache.json` |
| **缓存结构** | `{"code": {"ebit": 元, "ebit_margin": %, "revenue": 元, "ttm_ebit": 元, ...}}` |
| **更新频率** | **每季度**（财报季后3-5天）或按需手动 |
| **抓取脚本** | `backend/fetch_ebit.py` |
| **运行方式** | `python backend/fetch_ebit.py`（增量模式，跳过已有缓存） |
| **节流策略** | 每5只休息0.3-0.6秒；每50只自动保存 |

---

## 4. 盈利筹码比例

| 项目 | 内容 |
|------|------|
| **数据源** | **新浪财经日K线API** |
| **API端点** | `https://money.finance.sina.com.cn/quotes_service/api/json_v2.php/CN_MarketData.getKLineData?symbol=sh600519&scale=240&ma=no&datalen=120` |
| **数据范围** | 最近120个交易日 |
| **算法** | 统计收盘价低于当前价的日成交量占比 |
| **公式** | 盈利筹码比例 = 盈利量 / 总成交量 × 100% |
| **缓存文件** | `data/chip_cache.json` |
| **更新频率** | **每日**（收盘后运行，约需30-40分钟） |
| **抓取脚本** | `backend/fetch_chip.py` |

---

## 5. 市值3年分位

| 项目 | 内容 |
|------|------|
| **数据源** | **新浪财经日K线API（同筹码数据）** |
| **API端点** | 同上，但 `datalen=800`（约3年3个月交易日） |
| **算法** | (当前价 - 近3年最低价) / (近3年最高价 - 近3年最低价) × 100% |
| **注意** | 市值与股价线性相关（股本不变时），所以直接用股价分位替代市值分位 |
| **缓存文件** | 同 `data/chip_cache.json`（字段: `mcap_percentile`） |
| **更新频率** | **每日**（同筹码数据，在一次运行中同时计算） |

---

## 6. 行业分类（申万77行业）

| 项目 | 内容 |
|------|------|
| **数据源** | **硬编码 + 种子缓存** |
| **行业列表** | 77个申万行业，映射到东财BK代码（硬编码在 server.py） |
| **行业股票池** | `data/industry_stocks.json`（每行业约15只热门股） |
| **生成脚本** | `backend/seed_stocks.py` |
| **生成方式** | 先尝试东财API(`push2.eastmoney.com`) → 不可用时用热门池 |
| **热门池** | 预置756只股票(77行业全覆盖) + 188只补充扫描股 |
| **补充扫描** | `data/new_stocks.json`（通过腾讯API批量验证代码有效性） |
| **更新频率** | **按需**（每年1-2次，当行业成分股重大变化时） |
| **备用方案** | 东财API可在用户本地网络启用时自动获取全量行业数据 |

---

## 7. 话题热度评分

| 项目 | 内容 |
|------|------|
| **数据源** | **同花顺热点数据** |
| **依赖库** | `a-stock-data` 技能中的 heat_score 模块 |
| **获取方式** | 通过同花顺公开API获取当日强势股 + 题材归因 |
| **评分范围** | 0-100分，基于涨跌幅/换手率/量比/题材强度等 |
| **更新频率** | **每30分钟**自动刷新（服务启动后自动运行） |
| **代码位置** | `backend/server.py` → 启动时 `@app.on_event("startup")` |

---

## 定期更新脚本

### 每日更新（推荐）
每天早上9:30开盘前运行以下命令：

```bash
# 1. 更新筹码+市值分位（基于昨日收盘数据）
cd /path/to/project/backend
../python fetch_chip.py

# 2. 视情况更新EBIT（季度财报期）
# ../python fetch_ebit.py
```

### 全量数据刷新
```bash
# 按顺序执行：
cd /path/to/project/backend

# 1. 全A股列表（服务重启自动加载，无需手动）
# 2. 行业股票池（仅当需要更新行业分类时）
python seed_stocks.py

# 3. EBIT数据
python fetch_ebit.py

# 4. 筹码+分位数据
python fetch_chip.py

# 5. 重启服务
# 停止 -> python server.py
```

### 注意
- 所有抓取脚本均为**增量模式**：跳过已有缓存的股票
- 如需强制刷新全部数据，删除对应的 `data/*.json` 缓存文件后重新运行
- 单个完整数据周期（全部脚本运行）约需 **2-3小时**
- 新浪API有隐式限流，脚本已内置节流机制（每5只休眠），**请勿同时运行多个脚本**
